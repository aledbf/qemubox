name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g., v1.2.3). Defaults to the ref name."
        required: false

permissions:
  contents: write

jobs:
  release:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Task
        uses: go-task/setup-task@v1

      - name: Prepare buildx cache
        run: |
          sudo mkdir -p /var/lib/qemubox-buildkit-cache
          sudo chown -R "$USER":"$USER" /var/lib/qemubox-buildkit-cache

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: image=moby/buildkit:latest

      - name: Build release tarball
        env:
          VERSION: ${{ inputs.version || github.ref_name }}
        run: task release

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: _output/release/*.tar.gz
